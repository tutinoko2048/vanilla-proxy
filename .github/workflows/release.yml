name: Build and Release

on:
  push:
    branches:
      - release

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Go
        uses: actions/setup-go@v2
        with:
          go-version: "1.22.4"

      - name: Build [Windows]
        run: GOOS=windows GOARCH=amd64 go build -o vanilla-proxy-windows.exe .

      - name: Build [Linux]
        run: GOOS=linux GOARCH=amd64 go build -o vanilla-proxy-linux .

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: v${{ github.run_number }}
          release_name: Release ${{ github.run_number }}
          draft: false
          prerelease: false

      - name: Upload Release Asset [Windows]
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./vanilla-proxy-windows.exe
          asset_name: vanilla-proxy-windows.exe
          asset_content_type: application/octet-stream

      - name: Upload Release Asset [Linux]
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: ./vanilla-proxy-linux
          asset_name: vanilla-proxy-linux
          asset_content_type: application/octet-stream
